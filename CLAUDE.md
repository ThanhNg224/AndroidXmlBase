# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project State (read this first)

This repo is a **reusable Android base project under construction**, not a finished app. It was generated by Android Studio as a Compose starter template and is being converted into an **XML + ViewBinding, MVVM + Clean Architecture** base per `docs/BASE_PROJECT_PORT_PLAN.md`.

As of now the codebase still looks like the stock Compose template (`MainActivity.kt` + `ui/theme/*`, `buildFeatures.compose = true`, Compose BOM in `gradle/libs.versions.toml`). **Do not treat this as the target architecture** — treat `docs/BASE_PROJECT_PORT_PLAN.md` as the source of truth for where the project is going, and check current source under `app/src/main/java/com/example/androidxmlbase/` to see how far the port has actually progressed before assuming any core module (`core/architecture`, `core/network`, `core/storage`, etc.) already exists.

Reference project the plan ports ideas from (do not copy blindly, and never touch this path):
```
/Users/thanhng224/Dev/Kalapa/heyjapan-android-main
```

Read `docs/BASE_PROJECT_PORT_PLAN.md` in full before starting any port-related work — it defines phases (Phase 0–8), what to extract vs. rewrite vs. drop from the reference project, and acceptance criteria per phase. Work one phase at a time and don't jump ahead.

## Commands

```bash
./gradlew :app:assembleDebug        # build debug APK
./gradlew :app:assembleRelease      # build release APK
./gradlew test                      # run JVM unit tests (app/src/test)
./gradlew connectedAndroidTest      # run instrumented tests (app/src/androidTest), needs device/emulator
./gradlew :app:testDebugUnitTest --tests "com.example.androidxmlbase.SomeTest"   # run a single unit test class
./gradlew :app:testDebugUnitTest --tests "com.example.androidxmlbase.SomeTest.someMethod"  # run a single test method
```

There is no configured lint/format task beyond the Android Gradle Plugin defaults (`./gradlew lint`); no ktlint/detekt config exists in this repo yet.

## Architecture

Single Gradle module (`app`) today. The port plan calls for staying single-module with strict packages first, splitting into Gradle modules only after `core` stabilizes (see "Open Decisions" in the port plan) — don't introduce multi-module Gradle structure unless asked.

Target dependency direction (once ported):
```
UI -> ViewModel -> UseCase -> Repository interface -> RepositoryImpl -> DataSource/API/DB/Storage
```

Layer rules (from `docs/ARCHITECTURE.md`, `docs/STANDARD.md`, `AGENTS.md`):
- **Presentation**: renders UI, observes state, handles input/navigation. No API/DB access, no business rules.
- **Domain**: business rules, UseCases, entities, repository *interfaces*. No Android framework dependency.
- **Data**: repository *implementations*, remote/local data sources, mappers. No UI logic; never leak API/DB models to Presentation.
- Dependencies point inward only (Presentation → Domain → Data is the call direction; Data may depend on Domain only for repository interfaces). Feature modules must not depend on each other directly; shared/core code must not depend on feature code, and only moves into `core`/`shared` after proven reuse (2+ cases).

Target per-feature folder structure (see port plan for the authoritative version):
```
feature/<name>/
  data/{datasource,dto,mapper,repository}/
  domain/{entity,repository,usecase}/
  presentation/{model,viewmodel,ui}/
```

Target core folders: `core/architecture`, `core/common`, `core/storage`, `core/network`, `core/localization`, `core/designsystem`, `core/ui`, `core/analytics`, `core/navigation`, `core/logging`.

UI state convention (once `core/architecture` exists): explicit `UiState` / `UiEvent` / `UiEffect`, one-way data flow, one-time effects (navigation, toasts) delivered via `UiEffect` and never as sticky state.

### Do not port into this base (per the port plan)

Even though the reference HeyJapan project is the source of ideas, never bring across: its lesson/learning-content domain, IAP/paywall/sale logic, ads-specific logic, DBFlow legacy setup, concrete Firebase/Facebook analytics clients, the legacy singleton `Preference`, large mixed-responsibility helpers, or feature-specific remote config keys. When porting a concept, follow the plan's rule: copy only if generic and already clean, rewrite if the idea is strong but the implementation is app-specific, drop if product-specific or legacy-only.

## Working conventions (from AGENTS.md / docs/STANDARD.md)

- Before adding any Activity, Fragment, ViewModel, UseCase, Repository, Mapper, Adapter, Dialog, or Custom View, search the codebase for an existing one to extend/reuse first.
- Naming: classes `PascalCase`; functions/variables `camelCase`; constants `UPPER_SNAKE_CASE`; resources/layouts/drawables/colors/dimens `lowercase_with_underscores`.
- Kotlin: prefer `val`, avoid `!!`, use sealed classes for finite UI state, avoid `GlobalScope`, use structured concurrency and `viewModelScope`.
- No hardcoded `dp`/`sp` in XML layouts once the sdp/ssp convention lands (Phase 5) — use `@dimen/_<n>sdp` / `_<n>ssp`. No hardcoded hex colors in layouts except launcher assets. All user-facing text goes through string resources.
- Keep refactors scoped to what's requested; don't rewrite unrelated files or introduce abstractions without a repeated, proven need.
